name: My First workflow
on: push
jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My first github actions"
            
            - name: List files
              run: ls
            
            - name: Read files
              run: cat README.md
            
